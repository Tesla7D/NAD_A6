<%= form_for(@codepost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <div class="field">
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>

    <div id="html_field" class="field">
      <%= f.label :html,"Text" %>
      <%= f.text_area :html %>
    </div>

    <div class="actions">
      <%= f.submit "Post"%>
    </div>
<% end %>
